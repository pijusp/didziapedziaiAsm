console.log("Hello!!");

const array = Array(10).fill().map(() => Math.ceil(10 * Math.random()));

// for (var i = 0; i < array.length; i++) {
//     array[i] = array[i].toFixed(0);
// }
console.log(array);
